# ============================================================
# 白名单模式：只保留 src 目录 + blended 相关文件
# 其余所有文件/目录（含 src 之外的文件夹）一律忽略
# ============================================================

# 先忽略所有内容
*

# --- 重新放行（! 表示不忽略）---


# 保留 src 目录及其全部内容
!src/
!src/**

# 保留 blended 相关文件
!blended_run.py
!blended_*.ipynb

